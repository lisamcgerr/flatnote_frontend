//import actions

export default function usersReducer(users = [], action){
    switch(action.type){
        case "":
            return 
               

        default:
            return users
    }
}